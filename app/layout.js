import './globals.css';

export const metadata = {
  title: 'Video3MB — 1080×1440 video tayyorlash',
  description: 'Videoni qirqish, crop, watermark, ovoz va 3 MB gacha siqish.'
};

export default function RootLayout({ children }) {
  return <html lang="uz"><body>{children}</body></html>;
}